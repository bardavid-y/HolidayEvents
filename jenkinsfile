pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {
                echo "pull from github"
                checkout scm
            }
        }
        stage('install') {
            steps {
                echo "installing dependencies"
                sh "npm ci"
            }
        }
        stage('build and push') {
            steps {
                echo 'building docker image and pushing'
                sh "docker build -t HolidayEvents/holiday-jenkins:${BUILD_NUMBER} ."
            }
        }
        stage('deploy') {
            steps {
                echo "deploy the new version"
                // Changed to triple double-quotes (""") to allow ${BUILD_NUMBER} to work
                sh """
                    docker stop HolidayEvents || true
                    docker rm HolidayEvents || true
                    # Updated to exactly match the built image tag
                    docker run -d --name HolidayEvents -p 3000:3000 HolidayEvents/holiday-jenkins:${BUILD_NUMBER}
                """
            }
        }
    } 
    post {
        success { 
            echo "CI/CD pipeline deployment successful" 
        }
        failure { 
            echo "Failed to deploy" 
        }
    }
}